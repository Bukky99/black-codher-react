import React from 'react';
import ReactDOM from 'react-dom'; //rendering engine, what is showed on the webpage
import App from './App';

ReactDOM.render(
 <React.StrictMode>
 <App />,
 </React.StrictMode>,
 document.getElementById('root')
);

